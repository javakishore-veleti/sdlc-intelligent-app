# Workspace-Service configuration

# Database. Local default; Postgres in containers.
DATABASE_URL=sqlite:///./workspace.db
# DATABASE_URL=postgresql+psycopg2://sdlc:sdlc@postgres:5432/workspace

# Comma-separated admin emails (admins see all projects).
ADMIN_EMAILS=admin@example.com
