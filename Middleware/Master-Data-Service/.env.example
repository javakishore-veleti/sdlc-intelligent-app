# Master-Data-Service configuration

# Local default (zero external dependencies):
DATABASE_URL=sqlite:///./master_data.db

# PostgreSQL (containerized / production):
# DATABASE_URL=postgresql+psycopg2://sdlc:sdlc@postgres:5432/master_data
